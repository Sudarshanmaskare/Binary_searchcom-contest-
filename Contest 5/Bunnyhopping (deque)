You are given a list of integers nums and an integer k. The nums list represents the costs of landing at each index. 
You must start at index 0 and end at the last index of nums. In each step you can move from some position X to any position up to k steps away.

You want to minimize the sum of the costs of the positions you land at. What is this minimum sum?

()

int solve(vector<int>& nums, int k) {
    int n=nums.size();
    vector<int> dp(n);
    dp[0]=nums[0];
    deque<int> dq;
    dq.push_back(0);
    for(int i=1;i<n;i++){
        while(!dq.empty() && dq.front()<i-k) dq.pop_front();

        dp[i]=nums[i]+dp[dq.front()];

        while(!dq.empty() && dp[dq.back()]>=dp[i]) dq.pop_back();
        dq.push_back(i);
    }
    return dp[n-1];
}
